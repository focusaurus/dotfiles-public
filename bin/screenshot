#!/usr/bin/env bash

# Please Use Google Shell Style: https://google.github.io/styleguide/shell.xml

# ---- Start unofficial bash strict mode boilerplate
# http://redsymbol.net/articles/unofficial-bash-strict-mode/
set -o errexit  # always exit on error
set -o errtrace # trap errors in functions as well
set -o pipefail # don't ignore exit codes when piping output
# set -x          # enable debugging

IFS="$(printf "\n\t")"
# ---- End unofficial bash strict mode boilerplate
if [[ "$1" == "delay" ]]; then
  sleep 5
  shift
fi
macos_dir="/Applications/flameshot.app/Contents/MacOS"
if [[ -e "${macos_dir}/flameshot" ]]; then
  export PATH="${macos_dir}:${PATH}"
elif [[ "$(uname)" == "Darwin" ]]; then
  cliclick kd:cmd,shift t:5 ku:cmd,shift
  exit
fi
destdir="${HOME}/screens"
mkdir -p "${destdir}"
suffix="$1"
timestamp=$(date '+%Y-%m-%d-%H%M%S')
destfile="screen-${timestamp}-${suffix}.png"
out="${destdir}/${destfile}"
tool=flameshot
if [[ "${XDG_SESSION_TYPE}" == "wayland" ]]; then
  tool=grim
  # tool=niri
fi
case "${tool}" in
niri)
  niri msg action screenshot-window
  ;;
grim)
  # slurp selects the screen area to capture
  # grim captures it
  # satty annotates and saves it
  # grim -g "$(slurp)" - | swappy --file - --output-file - > "${out}"
  grim -g "$(slurp)" - | satty --filename - --output-filename - > "${out}"
  ;;
hyprshot)
  hyprshot --mode region --output-folder "${destdir}" --filename "${destfile}" || true
  ;;
flameshot)
  declare -a args

  args+=(gui --path "${out}")
  flameshot "${args[@]}"
  ;;
esac

echo "${out}"

#------- code graveyard

# if [[ "$(uname)" == "Darwin" ]]; then
#   cliclick kd:cmd,shift  t:x ku:cmd,shift
#   exit
# fi
# suffix=$(zenity --entry --text suffix)
# suffix=$(yad --entry --title "suffix" --text "suffix" --width 500)
# if command -v spectacle >/dev/null 2>&1; then
#   spectacle
#   # spectacle is configured to save as ~/screens/screen-YYYY-mm-dd-NN.png
#   spec_path=$(ls ~/screens/screen-*.png | tail -1)
#   if [[ -f "${spec_path}" ]]; then
#     mv "${spec_path}" "${out}"
#   fi
# else
