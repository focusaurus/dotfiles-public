# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# Main i3 modifier key is alt
# See readme.md in dotfiles-public for reasoning
set $mod Mod1
set $super Mod4
set $alt Mod1

workspace_layout tabbed
focus_follows_mouse no

##### font #####
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 10
#font pango:DejaVu Mono 16
#font pango:Noto Sans Mono 16
font pango:Inconsolata, DejaVu 12

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- "${HOME}/bin/lock-screen"

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+shift+Return exec i3-sensible-terminal

# kill focused window
#bindsym $mod+k kill
#bindsym $mod+Delete kill
bindsym Ctrl+Shift+w kill

##### focus a window #####
# arrow keys
bindsym $mod+Left mark LAST;focus left
bindsym $mod+Up mark TEMP; [con_mark="LAST"] focus; [con_mark="TEMP"] mark LAST
bindsym $mod+Right mark LAST;focus right
bindsym $mod+Down focus down
# right hand home row
bindsym $mod+h mark LAST;focus left
bindsym $mod+t mark TEMP; [con_mark="LAST"] focus; [con_mark="TEMP"] mark LAST
bindsym $mod+n mark LAST;focus right
bindsym $mod+w focus down
# left hand home row
bindsym $mod+o mark LAST;focus left
bindsym $mod+e mark TEMP; [con_mark="LAST"] focus; [con_mark="TEMP"] mark LAST
bindsym $mod+u mark LAST;focus right
bindsym $mod+j focus down



# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# split in horizontal orientation
bindsym $mod+bar split h

# split in vertical orientation
bindsym $mod+minus split v

# enter fullscreen mode for the focused container
# bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
bindsym $mod+shift+a layout tabbed
#bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

##### app nav #####
# right hand home row
#bindsym $super+h exec ~/bin/app-nav left
#bindsym $super+t exec ~/bin/app-nav up
#bindsym $super+n exec ~/bin/app-nav right
#bindsym $super+w exec ~/bin/app-nav down
#
## left hand home row
#bindsym $super+o exec ~/bin/app-nav left
#bindsym $super+e exec ~/bin/app-nav up
#bindsym $super+u exec ~/bin/app-nav right
#bindsym $super+j exec ~/bin/app-nav down
#
#
## alternatively, you can use the arrow keys:
#bindsym $super+Left exec ~/bin/app-nav left
#bindsym $super+Up exec ~/bin/app-nav up
#bindsym $super+Right exec ~/bin/app-nav right
#bindsym $super+Down exec ~/bin/app-nav down
#
#bindsym $super+space exec ~/bin/fuzz-script-choose
#bindsym Ctrl+1 exec ~/bin/fuzz-script-choose
#bindsym Ctrl+2 exec ~/bin/fuzz-snippet
#bindsym Ctrl+3 exec rofi -show run
#bindsym Ctrl+4 exec rofi -show window
#
#bindsym $super+v exec copyq show
#bindsym F1 exec ~/bin/fkeys f1
#bindsym Ctrl+F1 exec ~/bin/fkeys ctrl+f1
#bindsym F2 exec ~/bin/fkeys f2
#bindsym F3 exec ~/bin/fkeys f3
#bindsym F4 exec ~/bin/fkeys f4
#bindsym F5 exec ~/bin/fkeys f5
#bindsym F6 exec ~/bin/fkeys f6
#bindsym Shift+F6 exec ~/bin/fkeys shift+f6
#bindsym Ctrl+F6 exec ~/bin/fkeys ctrl+f6
#bindsym F7 exec ~/bin/fkeys f7
#bindsym F8 exec ~/bin/fkeys f8
#bindsym F9 exec ~/bin/fkeys f9
#bindsym F10 exec ~/bin/fkeys f10
#bindsym F11 exec ~/bin/fkeys f11
#bindsym F12 exec ~/bin/fkeys f12
#bindsym Ctrl+F12 exec ~/bin/fkeys ctrl+f12
#
#bindsym XF86MonBrightnessDown exec sudo brightnessctl set 20%-
#bindsym XF86MonBrightnessUp exec sudo brightnessctl set 20%+
#bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +10% && killall -SIGUSR1 i3status
#bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -10% && killall -SIGUSR1 i3status
#bindsym XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle && killall -SIGUSR1 i3status
#bindsym XF86AudioMicMute exec pactl set-source-mute @DEFAULT_SOURCE@ toggle && killall -SIGUSR1 i3status




# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+Escape exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym t resize grow height 10 px or 10 ppt
        bindsym w resize shrink height 10 px or 10 ppt
        bindsym n resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}
bindsym $mod+shift+z mode "resize"

#mode "focus" {
#  bindsym l focus left; mode "default"
#  bindsym r focus right; mode "default"
#  bindsym u focus up; mode "default"
#  bindsym d focus down; mode "default"
#}
#bindsym $mod+f mode "focus"

mode "i3-2nd-tier" {
        ##### focus a window #####
        # arrow keys
        bindsym Left mark LAST; focus left
        bindsym Up mark TEMP; [con_mark="LAST"] focus; [con_mark="TEMP"] mark LAST
        bindsym Right mark LAST; focus right
        bindsym Down mark LAST; focus down
        # right hand home row
        bindsym h mark LAST; focus left
        bindsym t mark TEMP; [con_mark="LAST"] focus; [con_mark="TEMP"] mark LAST
        bindsym n mark LAST;focus right
        bindsym w mark LAST; focus down
        # left hand home row
        bindsym o mark LAST; focus left
        bindsym e mark TEMP; [con_mark="LAST"] focus; [con_mark="TEMP"] mark LAST
        bindsym u mark LAST; focus right
        bindsym j mark LAST; focus down

        ##### move a window #####
        # arrow keys
        bindsym Shift+Left move left
        bindsym Shift+Up move up
        bindsym Shift+Right move right
        bindsym Shift+Down move down
        # left hand home row
        bindsym Shift+o move left
        bindsym Shift+e move up
        bindsym Shift+u move right
        bindsym Shift+j move down
        # right hand home row
        bindsym Shift+h move left
        bindsym Shift+t move up
        bindsym Shift+n move right
        bindsym Shift+w move down

        ##### container layout #####
        bindsym a layout tabbed
        bindsym s split h
        bindsym - split v

        #bindsym $mod+s scratchpad show
        #bindsym $mod+Shift+s mark LAST; move scratchpad
        # arrow keys
        bindsym Control+Left workspace prev
        bindsym Control+Up move to workspace next
        bindsym Control+Right workspace next
        bindsym Control+Down move to workspace prev
        # left hand home row
        bindsym Control+o, workspace prev
        bindsym Control+e, move to workspace next
        bindsym Control+u, workspace next
        bindsym Control+j, move to workspace prev
        # right hand home row
        bindsym Control+h, workspace prev
        bindsym Control+t, move to workspace next
        bindsym Control+n, workspace next
        bindsym Control+w, move to workspace prev

        # switch to workspace
        bindsym 1 workspace number $ws1
        bindsym 2 workspace number $ws2
        bindsym 3 workspace number $ws3
        bindsym 4 workspace number $ws4
        bindsym 5 workspace number $ws5
        bindsym 6 workspace number $ws6
        bindsym 7 workspace number $ws7
        bindsym 8 workspace number $ws8
        bindsym 9 workspace number $ws9
        bindsym 0 workspace number $ws10

        ##### move focused container to workspace #####
        bindsym Shift+1 move container to workspace number $ws1
        bindsym Shift+2 move container to workspace number $ws2
        bindsym Shift+3 move container to workspace number $ws3
        bindsym Shift+4 move container to workspace number $ws4
        bindsym Shift+5 move container to workspace number $ws5
        bindsym Shift+6 move container to workspace number $ws6
        bindsym Shift+7 move container to workspace number $ws7
        bindsym Shift+8 move container to workspace number $ws8
        bindsym Shift+9 move container to workspace number $ws9
        bindsym Shift+0 move container to workspace number $ws10

        ##### switch back to default mode #####
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym space mode "default"

}
bindsym $mod+i mode "i3-2nd-tier"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status
}

for_window [class="(?i)fxlauncher"] floating disable
for_window [class="(?i)yad"] floating enable
for_window [title="(?i)event tester"] floating enable
for_window [class="(?i)copyq"] floating enable
for_window [class="(?i)tilda.Tilda"] floating enable
for_window [class="(?i)zoom"] floating disable
#for_window [title="kazam.Kazam"] floating disable
#for_window [title="Zoom Meeting"] floating disable

exec --no-startup-id ~/bin/setup-desktop-environment
